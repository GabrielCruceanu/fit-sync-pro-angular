export interface OnboardingData {
  name: string;
  email: string;
  password: string;
  // Add any other fields required for onboarding
}

export interface OnboardingPersonalDetails {
  firstName: string;
  lastName: string;
  birthDate: string;
  gender: string;
}
